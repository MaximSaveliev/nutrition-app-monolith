# Backend API URL (OPTIONAL - only needed for server-side calls)
# Frontend automatically uses window.location.origin + /api (like SignUp)
# 
# Development: Defaults to http://localhost:8000
# Production: Set only if you need server-side API calls to external backend
# NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
